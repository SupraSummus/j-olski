"""Rule packs.

Every module here declares a module-level ``pack`` and hangs its rules off it.
Anything importable that does the same can be loaded from the command line with
``--packs``, so a project can keep its own pack outside this package.
"""

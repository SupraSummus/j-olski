#  The extraction's fixture is malformed on purpose: a setext heading, an image
#  with no description, a table nobody would lay out that way. It is input to a
#  program rather than prose anybody reads, so the prose linter has nothing to
#  say about it.
tests/fixtures/

#  Kopia prozy, z której `python3 -m dokumentacja` buduje stronę. Check puszczony
#  po budowaniu zgłaszałby każdą usterkę dwa razy, raz w oryginale i raz w kopii.
_dokumentacja/

#  Korpus audytowy, klonowany do katalogu repozytorium poleceniem, które drukuje
#  docs/audit-corpus.md. Jest cudzym Markdownem, więc check puszczony po jego
#  pobraniu zgłasza cudze usterki. Pozostałe pobierane korpusy stoją w
#  .gitignore i nie ma ich tutaj, bo Markdownu nie niosą.
ksef-docs/
rit-dokumentacja/

"""The calibration harness: what turns a corpus into something olski can read.

Milestone 0 keeps document formats out of the linter, so a corpus in a markup
format reaches the rules through here rather than through olski. What the
extraction invents by doing that is docs/extraction.md, since a transformation
rules fire on owes an account exactly as a rule owes a false-positive rate.
"""
